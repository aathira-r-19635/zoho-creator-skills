# Playwright MCP logs and snapshots
.playwright-mcp/
*.log
snapshot.md

# OAuth tokens and MCP config (sensitive - never commit)
mcp-oauth-tokens.json
oauth_creds.json
.zcode/ai/mcp.json

# All debugging screenshots (do not commit)
*.png

# App-specific documentation output and scratch captures (not committed —
# contains client app screenshots, generated PDFs/DOCX, and raw page snapshots).
# Covers wipro-doc/, addax-doc/, utcl-doc/, dsce-doc/, and any future <app>-doc/ folder.
*-doc/

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Node
node_modules/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
